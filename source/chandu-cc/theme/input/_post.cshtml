@model IDocument

@{
    string excerpt = Model.GetString(Statiq.Html.HtmlKeys.Excerpt);
    string imageUrl = "/images/" + Model.GetString(WebKeys.Image);
    string authroImage = "../../authorAvathors/chandrakanth.jpg";
}

<div class="">
    <a href="@Model.GetLink()" class="block group">
        <div class="fade-section  relative m-2 rounded-xl bg-slate-100 dark:bg-slate-800 dark:text-white shadow-md overflow-hidden hover:bg-slate-200 dark:hover:bg-slate-900">

            <div class="flex flex-col md:flex-row md:justify-between p-4 md:p-6 gap-4">

                @* Mobile image *@
                @if (imageUrl != "/images/" || Model.GetString("ImageUrl") != null)
                {
                    <div class="md:hidden flex-shrink-0">
                        <img class="object-cover rounded-lg w-full max-h-60" src="@(imageUrl != "/images/" ? imageUrl : Model.GetString("ImageUrl"))" alt="Model Cover">
                    </div>
                }


                @* Text content *@
                <div class="flex-1 flex flex-col justify-between">
                    <div class="flex flex-col space-y-2">
                        <div class="flex flex-wrap items-center gap-2 text-xs font-semibold text-neutral-500 dark:text-neutral-400">
                            <span class="px-2 py-1 bg-pink-100 dark:bg-pink-900 rounded-full border border-pink-200 dark:border-pink-700">@Model.GetString("Tags")</span>
                            <span>@Model.GetDateTime(WebKeys.Published).ToLongDateString()</span>
                        </div>

                        <h2 class="text-2xl font-bold capitalize">@Model.GetString("title")</h2>

                        @if(Model.ContainsKey("Lead"))
                        {
                            <p class="text-sm text-neutral-700 dark:text-neutral-300">@Model.GetString("Lead")</p>
                        }

                        <div class="text-sm text-neutral-600 dark:text-neutral-400">@Html.Raw(excerpt)</div>
                    </div>

                    @* Footer with hover highlight on "Read" *@
                    <div class="mt-4 flex items-center gap-3">
                        <div class="flex items-center space-x-1 px-3 py-1 rounded-md font-medium 
                                    bg-blue-600 text-white shadow-md 
                                    group-hover:bg-blue-700 group-hover:shadow-lg">
                            <span>Read More</span>
                            <svg class="h-4 w-4 fill-white" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
                                <path fill-rule="evenodd" d="M16.28 11.47a.75.75 0 0 1 0 1.06l-7.5 7.5a.75.75 0 0 1-1.06-1.06L14.69 12 7.72 5.03a.75.75 0 0 1 1.06-1.06l7.5 7.5Z" clip-rule="evenodd" />
                            </svg>
                        </div>

                    </div>
                </div>

                @* Desktop image *@
                @if (imageUrl != "/images/" || Model.GetString("ImageUrl") != null)
                {
                    <div class="hidden md:block flex-shrink-0 max-w-xs">
                        <img class="object-cover rounded-xl w-full max-h-72" src="@(imageUrl != "/images/" ? imageUrl : Model.GetString("ImageUrl"))" alt="Model Cover">
                    </div>
                }
            </div>
        </div>
    </a>
</div>


